--create a virtual environment inside UI, 
python -m venv .venv

Actiavte it
.venv\Scripts\activate

pip install flask

pip install pywebview

pip install waitress

pip install requests

pip install psutil


To run the App, py run.py